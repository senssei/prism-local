## What and why

## How it was tested
- [ ] `PYTHONPATH=. python3 -m unittest discover -s tests` passes
- [ ] New behavior has hermetic tests (no GPU/network needed)
- [ ] Docs and `CHANGELOG.md` updated if user-visible
